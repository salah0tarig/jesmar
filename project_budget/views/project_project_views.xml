<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Project form: Budget button opens budget lines (Outcome/Output) for this project -->
    <record id="view_project_project_form_budget" model="ir.ui.view">
        <field name="name">project.project.form.budget</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_budget_lines"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-money"
                        invisible="not account_id">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Budget</span>
                        <span class="o_stat_value">
                            <field name="total_budget_amount" widget="monetary"/>
                        </span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
